# differential-rs
 Simple forward differentiation

## Building Typst documents

Install the latest Typst compiler, for example:
```sh
cargo install --git https://github.com/typst/typst --tag v0.11.0-rc1
```
and then build the pdf:
```sh
typst compile doc/main.typ --root .
```